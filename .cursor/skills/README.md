# Skills projet (Shelf)

Ces skills sont partagés dans le repo (dossier `.cursor/skills/`) pour rendre l’agent plus constant sur les points délicats :

- MCP (tools/resources/prompts + API keys)
- EPUB three-way merge + writeback
- Recommandations personnalisées (scoring + privacy)

Chaque skill est dans un dossier avec `SKILL.md`.
