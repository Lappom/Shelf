import { test } from "@playwright/test";

test.describe("Reader & admin upload", () => {
  test.skip(
    true,
    "EPUB upload, reader progress, annotations, export MD — add fixtures + reader data-testid when ready",
  );
});
