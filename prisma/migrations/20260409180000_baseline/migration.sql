-- Baseline: schema already existed before Prisma Migrate was adopted (e.g. db push).
-- Mark this migration as applied without executing: `prisma migrate resolve --applied 20260409180000_baseline`
-- Then run `prisma migrate deploy` for subsequent migrations.
SELECT 1;
